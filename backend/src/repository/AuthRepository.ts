export const createNewUser = (username: string, password: string): number => {
    const userId = 3;
    return userId;
};

export const grantAdminPrivilege = async (userId: number) => {
    return;
};
